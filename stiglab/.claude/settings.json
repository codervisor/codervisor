{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "\"$CLAUDE_PROJECT_DIR\"/../scripts/cloud/stiglab.sh || { echo '--- fallback: cargo check + pnpm install ---'; cargo check --quiet 2>&1 | tail -5; pnpm install --prefer-offline --silent 2>&1 | tail -3; }",
            "timeout": 120,
            "statusMessage": "Setting up stiglab cloud environment..."
          }
        ]
      }
    ]
  }
}
